1. JavaScript Program to Find Square Root of a Number
let number = 25;
let result = Math.sqrt(number);
console.log(`The square root of ${number} is ${result}`);

2. JavaScript Program to Calculate Area of Triangle
let base = 5;
let height = 10;
let area = (base * height) / 2;
console.log(`The area of the triangle is ${area}`);

3. JavaScript Program to Solve Any Quadratic Equation
let a = 1, b = -3, c = 2;
let discriminant = b * b - 4 * a * c;

if (discriminant > 0) {
    let root1 = (-b + Math.sqrt(discriminant)) / (2 * a);
    let root2 = (-b - Math.sqrt(discriminant)) / (2 * a);
    console.log(`Roots are real and different: ${root1}, ${root2}`);
} else if (discriminant === 0) {
    let root = -b / (2 * a);
    console.log(`Roots are real and same: ${root}`);
} else {
    let realPart = (-b / (2 * a)).toFixed(2);
    let imagPart = (Math.sqrt(-discriminant) / (2 * a)).toFixed(2);
    console.log(`Roots are complex: ${realPart} + ${imagPart}i and ${realPart} - ${imagPart}i`);
}

4. JavaScript Program to Find Factorial of a Number
let number = 5;
let factorial = 1;

for (let i = 1; i <= number; i++) {
    factorial *= i;
}

console.log(`The factorial of ${number} is ${factorial}`);
